{
    "name": "root-package",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install --include=dev && npm run build",
        "start": "cd frontend && npx serve dist -s -l 5001"
    }
}